import { View, Text } from 'react-native';

export default function CameraStep() {
  return (
    <View style={{ alignItems: 'center', justifyContent: 'center' }}>
      <Text className={"text-red-600"}>Camera step</Text>
    </View>
  );
}
